#include <stdio.h>
int main(int argc, char const *argv[])
{
    int a=10;
    int b=20;
    //using realtional operaotrs on the variable on the variables a and b 
    // if its true then it will return 1 , if it is false then it will return 0
    printf("a>b: %i\n",a>b);
    printf("a<b: %i\n",a<b);
    printf("a<=b: %i\n",a<=b);
    printf("a>=b: %i\n",a>=b);
    printf("a==b: %i\n",a==b);
    printf("a!=b: %i\n",a!=b);




    return 0;
}
